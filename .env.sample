# Django settings
DEBUG=False
SECRET_KEY=your_secret_key_here
ALLOWED_HOSTS=paragoni.space,www.paragoni.space
CSRF_TRUSTED_ORIGINS=https://paragoni.space,https://www.paragoni.space
CSRF_COOKIE_SECURE=True
CSRF_COOKIE_HTTPONLY=True
CSRF_USE_SESSIONS=True
CSRF_COOKIE_SAMESITE=Strict

# Database settings
DB_NAME=movie_rating_db
DB_USER=movie_rating_user
DB_PASSWORD=your_strong_db_password
DB_HOST=db
DB_PORT=5432

# Superuser settings
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=your.email@example.com
DJANGO_SUPERUSER_PASSWORD=your_strong_admin_password

# Grafana settings
GRAFANA_ADMIN_PASSWORD=your_strong_grafana_password

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. Use strong, unique passwords for all credentials
# 3. Keep your secret key secure and never reuse it
# 4. This is a production environment template - use strong security settings
# 
# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual secure values
# 3. Ensure all hostnames and origins match your production setup 
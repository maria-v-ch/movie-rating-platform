# Django settings
DEBUG=True
SECRET_KEY=your-development-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
CSRF_COOKIE_SECURE=False
CSRF_COOKIE_HTTPONLY=False
CSRF_USE_SESSIONS=False
CSRF_COOKIE_SAMESITE=Lax

# Database settings
DB_NAME=movie_rating_db_dev
DB_USER=movie_rating_user_dev
DB_PASSWORD=your_dev_password_here
DB_HOST=db
DB_PORT=5432

# Superuser settings
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=your_dev_admin_password

# Grafana settings
GRAFANA_ADMIN_PASSWORD=your_dev_grafana_password

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. This is a development environment template with relaxed security
# 3. Do not use these settings in production
# 
# Instructions:
# 1. Copy this file to .env
# 2. Replace placeholder values with your development values
# 3. Keep development credentials different from production